# Student-Management-System-ITI-GUI
This repo contains the project of C++ and SQL Courses Final Project in ITI (GUI VERSION)

To solve QT CMake Error
```bash
sudo apt install qt6-base-dev qt6-base-dev-tools qt6-tools-dev qt6-tools-dev-tools
```